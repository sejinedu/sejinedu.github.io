# 세진 과학 — 밖에 올리지 않는 것
#
# ★ 여기 적힌 것은 형 작업실이다. 사이트가 도는 데는 필요 없다.

# 자막 만드는 공장 (파이썬·사전·칠판 그림 — 81MB)
자막만들기/

# 서버 (밖에서는 안 돈다. 파일만 주는 자리라서)
서버.js
켜기.vbs
켜기.vbs.바꾸기전

# 바꾸기 전에 남겨 둔 것
*.바꾸기전
*.되돌리기

# 지침 (사람 보는 글)
CLAUDE.md
이어받기.md

# 윈도우가 만드는 찌꺼기
Thumbs.db
desktop.ini
.DS_Store

# 올리는 단추 (형 컴퓨터에서만 쓴다)
올리기.bat
