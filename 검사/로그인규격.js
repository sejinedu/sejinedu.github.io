// ============================================================
//  로그인 화면 규격 대조 — 홈피 쪽  (2026-09-24)
// ============================================================
//  사장님: 「로그인 구성은 홈피와 런처가 동일하게」
//  원본: ..\0 세도비 지침서\로그인 화면 규격.md — 런처 test\검사.js 도 같은 파일을 읽어 맞춘다.
//  ★ 규격 파일의 목록을 **글자 그대로** 읽어 홈피 코드(js/로그인창.js)의 차례와 견준다.
//    한쪽만 바꾸면 여기서 빨강이 난다.
//  쓰는 법:  node 검사/로그인규격.js     (끝값 0 = 통과)

const fs = require("fs");
const path = require("path");
const vm = require("vm");

const 집 = path.join(__dirname, "..");
const 규격길 = path.join(집, "..", "0 세도비 지침서", "로그인 화면 규격.md");
const 규격 = fs.readFileSync(규격길, "utf8").replace(/\r/g, "");
const 창 = fs.readFileSync(path.join(집, "js", "로그인창.js"), "utf8");
const 회원 = fs.readFileSync(path.join(집, "js", "회원.js"), "utf8");

let 통과 = 0, 실패 = 0;
const 봄 = (됐나, 말) => { if (됐나) { 통과++; console.log("  ○ " + 말); } else { 실패++; console.log("  ✗ " + 말); } };
const 같나 = (가, 나) => JSON.stringify(가) === JSON.stringify(나);

// 규격 — 「## 제목」 밑의 첫 목록(- 로 시작하는 줄들)
function 목록(제목) {
  const 줄들 = 규격.split("\n");
  let ㅈ = 줄들.findIndex(ㄱ => ㄱ.trim() === "## " + 제목);
  if (ㅈ < 0) throw new Error("규격에 「" + 제목 + "」 이 없다");
  const 것 = [];
  for (ㅈ++; ㅈ < 줄들.length; ㅈ++) {
    const ㄱ = 줄들[ㅈ];
    if (/^## /.test(ㄱ)) break;
    if (/^- /.test(ㄱ)) 것.push(ㄱ.slice(2).trim());
    else if (것.length && ㄱ.trim() === "") break;          // 첫 목록이 끝났다
  }
  return 것;
}
// 홈피 코드 — const 이름 = [ ... ];
function 코드목록(이름) {
  const ㅁ = new RegExp("const " + 이름 + " = (\\[[^\\]]*\\]);").exec(창);
  if (!ㅁ) throw new Error("js/로그인창.js 에 " + 이름 + " 이 없다");
  return JSON.parse(ㅁ[1]);
}

console.log("로그인 화면 규격 대조 — " + 규격길);

// 1) 드롭바 · 로그인 창 차례
const 규격드롭바 = 목록("드롭바 (이름을 누르면)");
const 규격창 = 목록("로그인 창");
봄(같나(코드목록("드롭바차례"), 규격드롭바), "드롭바 차례가 규격과 같다 — " + 규격드롭바.join(" / "));
봄(같나(코드목록("로그인창차례"), 규격창), "로그인 창 차례가 규격과 같다 — " + 규격창.join(" / "));

// 2) 드롭바 머리 · 보이는 조건이 코드에 있나
봄(/드롭바차례\.forEach/.test(창), "드롭바는 드롭바차례 그대로 그린다 (따로 적은 글자 없음)");
봄(/"회원 관리": \{ 된다: ㅅ\.등급 === "admin"/.test(창), "회원 관리 — 관리자만");
봄(/"초대 관리": \{ 된다: !계정정지\(ㅅ\), 수: 받은초대수\(ㅅ\)/.test(창), "초대 관리 — 누구나 · 계정 정지면 안 보임 · 받은 초대 수");
봄(/ㄷ\.textContent = 글;\s*\n\s*if \(ㄱ\.수\)[^\n]*계정배지/.test(창), "초대 수는 배지로 따로 — 항목 글자는 「초대 관리」 그대로");

// 3) 이름 칸 — 「닉네임」 만 (채널 판) 을 실제로 돌려 본다
const 틀 = { 글로그인: "로그인" };
vm.createContext(틀);
vm.runInContext(창.slice(창.indexOf("  function 이름칸글"), 창.indexOf("  const 채널줄들")), 틀);
const 이름 = ㅅ => vm.runInContext("이름칸글(" + JSON.stringify(ㅅ) + ")", 틀);
봄(이름({ 들어왔나: false }) === "로그인", "로그인 전: 「로그인」");
봄(이름({ 들어왔나: true, 별명: "김세진", 메일: "a@b.c" }) === "김세진", "로그인 뒤: 「닉네임」");
봄(이름({ 들어왔나: true, 별명: "김세진", 메일: "a@b.c", 채널: { 목록: [{ 이름: "세진과학", 내것: false, 상태: "활성", 소속이름: "팀장" }] } }) === "김세진", "채널에 들어가 있어도 「닉네임」 만 — 채널·소속을 안 붙인다");
봄(이름({ 들어왔나: true, 별명: null, 메일: "tpwls455@gmail.com" }) === "tpwls455", "닉네임이 없으면 메일 앞부분");
봄(/className = "빨간점"/.test(창) && /받은초대수\(ㅅ\)/.test(창), "받은 초대가 있으면 이름 칸에 빨간 점");

// 4) 학원_나 · 앵커 · 비밀번호 찾기 → 새 비밀번호
봄(/채널_목록/.test(회원) && /await 채널읽기\(\)/.test(회원) && 규격.includes("rpc/채널_목록()"), "로그인 · 켤 때 rpc/채널_목록 을 부른다");
봄(규격.includes("#member-admin") && /member-admin\|post-pin/.test(회원), "앵커 #member-admin · #post-pin 을 받는다");
봄(/걸음보이기\("새비번"\)/.test(창) && /\/auth\/v1\/user", \{ 방법: "PUT"/.test(회원), "비밀번호 찾기 → 번호로 들어가기 → 새 비밀번호 정하기");
봄(/type: "signup"/.test(회원) && /type: "email"/.test(회원), "가입은 verify type signup · 찾기는 type email");
봄(/글자 수|length < 2 \|\| 별명\.length > 20/.test(회원), "닉네임 두 글자에서 스무 글자");

// 5) 초대 관리 — 규격 「## 초대 관리」 절 (채널 판: 받은 초대 · 들어간 채널 · 내 채널)
const 초대 = fs.readFileSync(path.join(집, "js", "초대관리.js"), "utf8");
const 초대절 = (() => { const ㅈ = 규격.search(/^## 초대 관리/m); if (ㅈ < 0) return ""; const 뒤 = 규격.slice(ㅈ + 3); const 끝 = 뒤.search(/^## /m); return 끝 < 0 ? 뒤 : 뒤.slice(0, 끝); })();
봄(!!초대절, "규격에 「## 초대 관리」 절이 있다");
const 초대단추들 = [...new Set([...초대절.split("권한은 **각 앱에서**")[0].matchAll(/`([^`]+)`/g)].map(ㅁ => ㅁ[1]).filter(ㄱ => !/[(…_]/.test(ㄱ) && !/^https?:/.test(ㄱ) && !/\.sql$/.test(ㄱ)))];
const 빠진단추 = 초대단추들.filter(ㄱ => !초대.includes('"' + ㄱ + '"'));
봄(초대단추들.length >= 7 && !빠진단추.length, "초대 관리 단추 이름이 규격 글자 그대로 — " + 초대단추들.join(" / ") + (빠진단추.length ? " (빠짐: " + 빠진단추.join(", ") + ")" : ""));
봄(["채널_받기", "채널_거절", "채널_나가기"].every(ㄱ => 초대.includes('"' + ㄱ + '", { p_주인: ㄱ.주인 }') && 규격.includes("rpc/" + ㄱ + "(p_주인)")), "받기 · 거절 · 나가기 — rpc 채널_받기/거절/나가기(p_주인)");
봄(/"학원_이름바꾸기", \{ p_이름: 새이름 \}/.test(초대), "내 채널 이름 바꾸기 — rpc 학원_이름바꾸기(p_이름)");
봄(/학원_초대", \{ p_메일: 메일, p_소속이름: "선생님", p_권한: \{ 반범위: "자기" \}, p_강사id: null \}/.test(초대) && 초대절.includes(`'선생님', {"반범위":"자기"}, null`), "새 메일은 학원_초대(메일, 선생님, 자기 반, null) — p_주인 없이 = 내 채널");
봄(/명단\.some\(사람 => [^\n]*=== 메일\)\) \{[\s\S]{0,200}?return;/.test(초대), "이미 명단에 있는 메일은 학원_초대를 다시 안 부른다");
// 초대 두 가지 — ① 채널로 초대(가입한 사람만 · 카톡 글 없음) ② 가입 안내 글 복사(서버 안 부름)
const 받는곳 = (/`(https:\/\/sejinedu\.github\.io\/download\.html)`/.exec(초대절) || [])[1];
const 안내글 = (/const 가입안내글 = \(\) => ([\s\S]*?);\r?\n/.exec(초대) || [])[1] || "";
봄(/초대는 \*\*두 가지\*\*/.test(초대절) && 초대.includes('"채널로 초대"') && 초대.includes('"가입 안내 글 복사"'), "초대는 두 가지 — 「채널로 초대」 · 「가입 안내 글 복사」");
봄(!/카톡/.test((/초대단\.addEventListener\("click"[\s\S]*?\n    \}\);/.exec(초대) || [""])[0]) && !/초대하고 카톡 글 복사/.test(초대), "채널로 초대는 카톡 글을 안 만든다 — 받은 사람 이름에 빨간 점");
봄(/가입하지 않은/.test(초대) && 초대.includes("아직 가입하지 않은 메일이다 — 아래 「가입 안내 글 복사」 로 먼저 가입하게 해라") && 초대절.includes("아직 가입하지 않은 메일이다 — 아래 「가입 안내 글 복사」 로 먼저 가입하게 해라"), "가입 안 한 메일 — 서버 거절을 규격 글자 그대로 띄운다");
봄(!!받는곳 && 안내글.includes("런처받는곳") && 초대.includes('"' + 받는곳 + '"') && !/sedobi-setup\.exe/.test(초대) &&
   ["[세도비] 가입 안내 — 「", "「로그인」 → 「회원 가입」", "가입한 메일을 알려 주면 채널로 초대해 드립니다"].every(ㄱ => 안내글.includes(ㄱ)), "가입 안내 글 — 받기 쪽 주소(exe 없음) · 로그인 → 회원 가입 · 가입한 메일을 알려 주면 초대");
봄(!/학원부르기/.test((/안내단\.addEventListener\("click"[\s\S]*?\n    \}\);/.exec(초대) || ["학원부르기"])[0]), "가입 안내는 서버를 안 부른다");
봄(fs.existsSync(path.join(집, "download.html")) && /sedobi-setup\.exe/.test(fs.readFileSync(path.join(집, "download.html"), "utf8")), "받기 쪽(download.html)이 있고 설치 파일로 이어진다");
봄(/"학원_빼기", \{ p_메일: 사람\.메일 \}/.test(초대) && !/p_주인: 내것/.test(초대), "탈퇴시키기 = 학원_빼기 (내 채널)");
봄(!/"권한"/.test(초대) && !/p_권한: 것\.권한/.test(초대), "「권한」 단추가 없다 — 권한은 각 앱에서");

// 6) 가입 끝 · 홈피 등급과 앱 자격은 따로 (채널 판)
const 가입줄 = 규격.split("\n").find(ㄱ => /^- 회원 가입:/.test(ㄱ)) || "";
봄(/어떻게 쓰나요\?」 는 없앴다/.test(가입줄) && !창.includes('"어떻게 쓰나요?"'), "가입 끝 「어떻게 쓰나요?」 는 없다 — 제 채널이 저절로 생긴다");
봄(!/site_me/.test((/async function 채널읽기\(\) \{[\s\S]*?\n  \}/.exec(회원) || [""])[0]), "채널을 읽은 뒤 홈피 등급을 다시 읽지 않는다 — 등급은 관리자만");
const 회원관리 = fs.readFileSync(path.join(집, "js", "회원관리.js"), "utf8");
봄(/학원관리_목록/.test(회원관리) && /학원관리_정지", \{ p_학원: ㅎ\.학원, p_정지: 정지 \}/.test(회원관리) && /\["학원", "채널"\]/.test(회원관리), "회원 관리 안에 채널 목록 · 정지(= 계정 정지)");
봄(!/선생관리|학원읽기/.test(창 + 회원) && !fs.existsSync(path.join(집, "js", "선생관리.js")), "옛 이름(선생 관리 · 학원읽기)이 남지 않았다");

console.log("\n통과 " + 통과 + " / 실패 " + 실패);
process.exit(실패 ? 1 : 0);
