# 인수인계 — 세도비 홈페이지 (2026-09-23 · 커뮤니티 개편을 끝내고 다시 적음)

> 코드를 읽으면 아는 건 안 적었다. **읽어도 모르는 것만** 적는다.
> 마지막 상태: **판 2.96** · 게시판(커뮤니티)로 갈아엎음 · 자료는 파일이 아니라 **저장소(Supabase)** 에 있다.
>
> ★ 2026-09-11 판 이어받기를 읽고 왔다면 **1번부터 다시 읽어라. 뼈대가 바뀌었다.**
>   그때는 「사이트가 둘」 이었다. 지금은 **하나**다.

---

## ★★★ 1. 사이트는 **하나**다 — 제일 중요한 칸

사용자가 정한 것 (2026-09-22): 「그냥 홈페이지는 하나라고 … 그냥 공개되어 있는 거라고 **커뮤니티 게시판처럼**」

```
https://sejinedu.github.io          ← 이것 하나가 사이트 전부다
http://localhost:8777               ← 화면이 아니다. 형 컴퓨터 안에서만 도는 **다리**다
```

| | 옛날 (~2.6x) | 지금 |
|---|---|---|
| 사이트 | 애들 보는 쪽 + 올리는 쪽, 둘 | **하나** |
| 자료 | `자료/영상목록.js` 같은 **파일** | **저장소(Supabase) 표** |
| 올리기 | 8777 화면에서만 | 공개 사이트에서 **로그인하고** 올린다 |
| 8777 | 올리는 화면 | **다리만** — 영상편집·자막·유튜브 → 저장소 |

★ **보는 것은 로그인 없이 된다. 이 줄을 건드리면 안 된다** (배틀넷 작전 307행, 사용자가 못 박음).
  로그인은 **쓰는 일**(글·댓글·올리기)에만 필요하다.

★ `파일로고치나 = false` (`js/앱.js:381`). **파일로 고치는 길은 은퇴했다.**
  옛 파일로 뜬 날(저장소가 죽은 날)에도 화면에서 고치지 않는다 — 보여 주기만 한다.

---

## 2. 저장소 지도 (Supabase · 세도비와 **같은 계정**)

사용자가 정한 것: 「야 아이디는 그냥 **세도비로 통합**한다」

```
project ref  burwsvkcaiqfiymdptex
표    site_members · site_units · site_posts · site_comments · site_captions
창구  site_posts_view · site_comments_view      ← 글쓴이 uid 를 안 내보낸다. 읽기는 늘 창구로
부름  site_me · site_my_role · site_is_admin · site_can_post
      site_set_role (관리자만) · site_admin_members (관리자만) · site_units_reorder (관리자만)
만든 것  db/1~5*.sql  (두드려보기.sql 은 권한을 22가지로 찔러 보고 전부 되돌린다)
```

★★ **권한은 화면이 아니라 RLS 가 막는다.** 화면에서 단추를 숨기는 건 보기 좋으라고다.
  개발자 도구로 단추를 되살려도 저장소가 거절한다 — 2026-09-22 에 익명 손님으로 찔러서 쟀다
  (`site_posts` 직접 읽기·`site_set_role`·`site_admin_members` 전부 `42501`).

★★ **공개 열쇠(publishable)만 코드에 넣는다.** `supabase.txt` 의 **SECRET_KEY 는 읽지도 마라.**
  공개 열쇠는 원래 밖에 보여도 되는 것이다 — 막는 건 열쇠가 아니라 RLS 다.

★ 세도비 다른 앱(학생 관리 등) 표와 **같은 방**을 쓴다. 안 새나 쟀다 —
  그 표들은 전부 `owner = auth.uid()` 라서 **학생 계정에서 형 학생 명단은 0줄**이다 (48줄 중 0).

---

## 3. 회원 · 등급

| 등급 | 하는 일 |
|---|---|
| 일반 회원 `member` | 보기 · 댓글 · 제 댓글 지우기 |
| 선생님 `teacher` | + 글(영상) 올리기 · 제 글 지우기 · 자막 넣기 |
| 관리자 `admin` | + 남의 글 지우기 · **등급 올려 주기** · 단원 순서 바꾸기 |

- 관리자는 **형 하나**다. 화면에서 남을 관리자로 못 올린다(저장소도 막는다).
- 등급 바꾸기: 이름 누르면 뜨는 드롭바 → **회원 관리** (`js/회원관리.js`).
- 로그인 = **세도비 런처에 넣는 그 메일·비밀번호**. 비밀번호를 잊으면 메일 여섯 자리 번호로 들어온다.
- 8777 화면은 **런처 로그인을 빌려 쓴다** (`GET /로그인/표`). 새로고침표는 절대 안 빌려준다 —
  화면이 새로 받으면 런처 쪽 표가 죽는다.
- ★ **구글 로그인 (2026-09-23 붙임).** 코드는 다 있다. 저장소에서 그 문을 열어 놓으면
  로그인 창에 단추가 **저절로** 생긴다 (`/auth/v1/settings` 를 물어보고 그린다).
  카카오도 같은 자리다 — 열기만 하면 단추가 하나 더 생긴다. **코드는 안 고쳐도 된다.**
  왜 필요하냐: 메일 번호는 **한 시간에 서른 통**이 한도다. 한 반이 한꺼번에 가입하면 거기서 막힌다.

### 3-1 구글 쪽 짜임 (2026-09-23 에 깔아 둔 것)

```
구글 클라우드 프로젝트가 둘이다 — 섞지 마라
  sejin-science   유튜브 가져오기용. 권한이 youtube.force-ssl(민감)이라
                  구글이 「확인 안 된 앱」 으로 본다 → 허락할 때 「고급 → 이동」 을 거쳐야 한다
  sejin-login     홈페이지 로그인용. 권한이 메일·이름뿐(민감 아님) → 경고도, 인원 제한도 없다
      클라이언트  「세진 과학 홈페이지 로그인」 (웹)
      돌아올 곳   https://burwsvkcaiqfiymdptex.supabase.co/auth/v1/callback
      원본        https://sejinedu.github.io
둘 다 **프로덕션 단계**로 게시했다 (2026-09-23).
  게시 조건이 홈페이지 URL + 개인정보처리방침 URL 이라 `개인정보.html` 을 그때 만들었다.
  ★ 게시해야 시험 상태의 **7일 허락 끊김**이 없어진다. 되돌리면 그 병이 돌아온다.
```
★ Supabase 쪽도 같이 고쳤다 — 사이트 주소가 `http://localhost:3000` 이었다(아무 데도 아닌 곳).
  `https://sejinedu.github.io` 로 고치고 돌아올 주소 목록에 `https://sejinedu.github.io/**` 를 넣었다.
★ 클라이언트 보안 비밀번호는 **사용자가 직접** 넣는다. 우리는 비밀번호·열쇠를 대신 입력하지 않는다.

---

## 4. 올리는 절차 — 고치고 나서 사이트에 뜨기까지

```
git add -A && git commit && git push      →  GitHub Pages 가 1~3분 뒤 반영
js/판.js 의 window.판 을 0.01 올린다        →  이게 없으면 브라우저가 옛 파일을 계속 쓴다
```
`index.html` 이 판으로 캐시를 턴다 (`판.js?t=<시각>` 를 먼저 받고 나머지는 `?v=<판>`).
**판 올리기는 선택이 아니다.** 사용자는 화면 오른쪽 위 **업데이트 단추**만 누른다.

반영됐나: `node 검사/자료가따라오나.js` · 또는 `https://sejinedu.github.io/js/판.js` 를 연다.

---

## 5. 8777 다리 — 무엇이 무엇을 넘기나

```
영상편집 「홈페이지에 연결하기」  →  옛 파일 모양으로 8777 에 떨어진다
                                  →  서버.js 글넘기기()  →  site_posts
자막만들기/ 의 완성 자막 48편      →  서버.js 자막넘기기() →  site_captions
유튜브 채널                       →  /유튜브/가져오기     →  site_posts + site_captions
                                  다리돌리기() 가 30초마다 스스로 돌린다
```
★ **영상편집은 딴 제품이다. 고치지 마라.** 그쪽이 옛 파일 모양으로 주는 것을 **여기서 받아 번역한다.**
★ 길은 **한 쪽**이다 — 「홈피에서 유튜브로 가는 것은 없애라」 (2026-09-22 사용자).

---

## 6. 자막

지금 자막 **48편 15MB** 는 다 만들어져 있다. 새로 굽는 건 새 강의를 올릴 때뿐이다.

- 받아적기 **Qwen3-ASR-1.7B + ForcedAligner**(로컬 4090) · 사전 `고칠말.txt`(2,400줄)·`공식.txt`·`과학용어.txt`
- 다듬기 `고치기.py` → `손보기/말끊기.py` → `손보기/시간바로잡기.py`
- ★ 끊는 규칙(사용자가 여러 날 스샷으로 잡아 준 것) — **글자 수를 세지 않는다.**
  · 자막 하나 = **3.5초쯤 말한 만큼**, 한 마디가 5.5초를 넘을 때만 쪼갠다
  · 글씨 크기는 그 강의에서 **제일 넓은 줄을 실제로 그려 재서** 맞춘다 (`js/자막.js`)
  · 마침표는 **화면에서만** 뗀다 (속으로는 갖고 있다 — 문장 끝을 아는 표다)
  · **말 가운데를 자르는 것보다 두 줄이 낫다.** 이건 사용자가 못 박았다

---

## 7. 유튜브 가져오기 — **허락이 7일이면 죽는다**

`자막만들기/유튜브가져오기.py` (읽기만 한다. 채널에는 아무것도 안 쓴다)
```
허락 / 목록 / 자막 <영상아이디>       ← 8777 이 부른다. 답은 JSON 한 줄
```
★ **구글이 「시험 상태」 앱의 허락을 7일마다 끊는다.** 그래서 `invalid_grant` 가 난다.
  화면에는 「유튜브 허락 받기」 단추가 뜨고, 그걸 **사용자가 눌렀을 때만** 구글 창이 뜬다
  (지침 2-3 — 사용자 화면에 창을 함부로 띄우지 마라).
  **영영 고치는 법:** 구글 클라우드 콘솔에서 그 앱을 **프로덕션으로 게시**한다. 그러면 안 끊긴다.
  ★ **2026-09-23 에 게시했다.** 그러니 이제 안 끊긴다 — 다만 그 전에 죽은 표가 남아 있어서
  사용자가 **한 번만** 「유튜브 허락 받기」 를 눌러야 한다. 그 뒤로는 안 물어본다.

---

## 8. 지뢰 (내가 밟은 것들 — 그대로 밟는다)

1. **★ bash heredoc 이 역슬래시를 먹는다.** 정규식·역슬래시가 든 코드는 **Edit/Write 도구로** 고쳐라. 여러 번 밟았다.
2. **bash 에서 한글 변수 이름은 안 된다.** `죽은=$(…)` 가 `command not found` 로 튄다.
3. **8777 을 죽일 땐 PID 로.** 한글 `-like` 로는 안 잡힌다 → `Get-NetTCPConnection -LocalPort 8777`.
   안 그러면 **옛 서버가 계속 답해서 시험 결과를 거짓으로 읽는다.**
4. **자모를 문자열로 쓰면 절대 안 걸린다.** `"멀어질".endswith("ㄹ")` 은 **False** 다.
5. **사전 규칙이 말 한가운데서 터진다.** 낱말로 덧대지 말고 **규칙에서 빼라.**
6. `%LOCALAPPDATA%`·`%APPDATA%` 에 클로드가 쓰면 **가상화돼서 진짜 프로세스한테 안 보인다.**
   산출물은 `C:\Users\tpwls\sedobi-local\` 에. `.git` 은 정션이다(구글 드라이브가 `.git` 을 못 삼킨다).
7. **★ 단추 모양을 한데 묶어 칠하면 `[hidden]` 을 덮는다.** v2.74 에서 숨겨 둔 머리줄 단추가
   **남의 화면에 몇 분 보였다.** → `.머리오른쪽 [hidden] { display: none !important; }` (v2.75).
   **묶어 칠하는 규칙을 쓸 땐 숨긴 것부터 세어 봐라.**
8. **위로 열리는 판은 `overflow: hidden` 에 잘린다.** 자막 크기 판이 통째로 사라졌다.
9. **`window.blur` 에서 `window.focus()` 를 부르면 유튜브 안쪽 드래그가 죽는다.**
   누를 때마다 iframe 에서 초점을 뺏어 온다 → 음량·탐색 막대가 안 끌린다.
10. **폰 화면은 띠 폭을 좁히지 마라.** 「끌어서 키우기」 가 화면 오른쪽 띠에서 일어난다 —
    v2.84 에서 그 띠를 반으로 줄여 **확대가 죽었다**(축소만 됐다). v2.89 에서 되돌렸다.
11. **격자 마지막 칸에는 `minmax(0,1fr)`** 을 줘라. 안 그러면 폰에서 표가 옆으로 넘친다.
12. **★★ 「멈춰라」 는 말을 들었을 때 — 이미 돈 것부터 재라.** 2026-09-22 에 거절된 명령이
    사실은 이미 돌아서 v2.90 이 올라가 있었는데 「안 올렸다」 고 말했다. **거짓 보고였다.**
    → 멈추라는 말을 들으면 **`git log` 부터 보고** 어디까지 갔는지 말해라.

---

## 9. 남은 것

1. ~~구글 로그인~~ — **2026-09-23 에 끝났다.** 저장소에 구글 문이 열렸고(`google:true`)
   사이트 로그인 창에 「구글 계정으로 계속하기」 가 뜬다. **눈으로 봤다.**
2. **유튜브 허락 다시 받기** — 화면의 「유튜브 허락 받기」 한 번. 게시는 끝냈으니 이번엔 안 끊긴다.
   (민감 권한이라 「확인되지 않은 앱」 화면이 뜬다 → 고급 → 이동)
3. 8777 다리의 **첫 진짜 실행** — 자막 48편이 `site_captions` 로 올라간다. 올라간 뒤 세어 봐야 한다.
4. 학생 기기에서 런처 `device_claim` 막기 (런처 쪽은 준비됨, 저장소 쪽이 남음).
5. 자막 공장이 **새 글을 스스로 집어 가기**.
6. 자막 자잘한 오류 — **규칙으로 고쳐라.** 줄 하나씩 손으로 고치면 다음 강의에서 또 난다.

---

## 10. 사용자 대하는 법 (여기가 제일 값지다)

- **코드를 못 읽는다.** 한국어로, **결론부터.** 코드 조각·영어 용어를 들이대지 마라.
- **절차를 시키지 마라.** 「재설치하세요」 「이 명령을 쳐 보세요」 는 답이 아니다.
  내가 재서 판단하고, 사용자는 **「업데이트」 한 번만** 누른다.
- **「고쳤다」 고 말하기 전에 눈으로 봐라.** 숨김 검사 통과는 증거가 아니다.
  안 재고 「됐다」 하면 **스샷이 다시 날아온다.** 「나 민감하다. 디자인 똑바로 해라」
- **영상 시험 전엔 반드시 음소거를 먼저 심어라.** 소리가 갑자기 나면 크게 화낸다.
- **사용자 화면에 창을 띄우지 마라.** 「넌 숨어서 해라」 — 시험은 숨은 브라우저로.
- 끝맺음은 셋으로 — **① 판 번호 ② 뭘 고쳤나 ③ 업데이트 눌러라**(받을 게 있을 때만).
- **내가 틀렸으면 짧게 인정하고 넘어가라.** 변명이나 자책을 길게 하면 더 화낸다.

---
---

# (아래는 그 전 기록 — 2026-09-10 Codex 세션)

- v2.58 고정 프레임 자막: 유튜브 기본 CC(1920×1080=48px, 960×540=24px)와 같은 프레임으로 맞췄다.
  Roboto 웹폰트와 OFL 라이선스를 넣어 영문·공백 폭까지 맞췄다.
- v2.57 자막 기본 크기: 공통 산식 `caption-layout.js` 로 옮겼다. 원본은 형제 폴더
  `세도비 영상편집/app/ui/caption-layout.js` 이고 홈페이지에는 **배포 사본**을 둔다.
  크기 열쇠는 `세진과학.자막크기.v2`.
