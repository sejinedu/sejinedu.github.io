# 인수인계 — 세도비 홈페이지 (2026-09-11 · 홈페이지 방이 조교 방으로 넘김)

> 코드를 읽으면 아는 건 안 적었다. **읽어도 모르는 것만** 적는다.
> 마지막 상태: 커밋 `2a48224` · **판 2.60** · 안 올린 것 0개 · Pages 반영 확인함.

---

## ★★★ 1. 사이트가 둘이다 — 제일 중요한 칸

사장님 말: 「애들 보는 사이트가 있고 **내가 업로드 하는 사이트**가 따로 있다」

**둘은 다른 서버가 아니다. 같은 폴더를 보는 두 입구다.**

| | 애들 보는 쪽 | 사장님이 올리는 쪽 |
|---|---|---|
| 주소 | `https://sejinedu.github.io` | `http://localhost:8777` |
| 무엇이 띄우나 | **GitHub Pages** | `켜기.vbs` → `node 서버.js` (사장님 PC) |
| 저장소 | `github.com/sejinedu/sejinedu.github.io` · 브랜치 `main` · **뿌리 폴더 그대로** | 같은 저장소의 **작업 폴더** |
| 작업 폴더 | — | `C:\구글 드라이브\0 세도비\세도비 홈페이지` |
| 로그인 | **없다. 있으면 안 된다** | `SEDOBI_TOKEN` 있어야 고치기·올리기가 열린다 |
| 고칠 수 있나 | 못 한다 (정적) | 고치고 올린다 |

### ★ 절대 하면 안 되는 것
**애들 보는 쪽에 로그인·차단을 걸지 마라.** 무료 공개 강의 사이트다. 벽을 세우면 학생이 못 본다.
2026-09-11 에 「모든 앱에 로그인 없으면 못 열게」 지시가 왔을 때, 사장님께 확인받아
**올리는 쪽에만** 걸었다. 이 갈래는 사장님이 직접 확정한 것이다.

### 로그인은 이렇게 흐른다
런처(세도비) → 환경변수 → `켜기.vbs` → `서버.js` 가 그대로 상속받는다.
**앱에 로그인 화면은 없다** (2단계 규격 60행). 없으면 멈출 뿐이다.
```
SEDOBI_TOKEN   ← 이게 살아 있어야 통과. 만료(JWT exp)도 본다
SEDOBI_EMAIL · SEDOBI_UID
```
잠긴 길: `/올리기` `/단원나무` `/영상목록` `/영상/길이` `/자막/만들기`
**안 잠근 것: 보기 전부** (`/`, `/js/*`, `/자막/*`). 미리보기는 로그인 없이 된다.
로그인 안 됐으면 화면 맨 위에 **빨간 띠**가 뜬다. 공개 사이트엔 안 뜬다(`/로그인` 길이 없어서).

★ **바탕화면 바로가기로 켜면 올리기가 막힌다.** 런처로 켜야 로그인이 넘어온다.
★ 2026-09-11 현재 **사장님이 아직 로그인을 안 하셨다.** 런처는 이미 넘기고 있다(런처 방 실측).
  사장님이 런처에서 로그인 한 번 하면 그때부터 열린다.

---

## 2. 올리는 절차 — 고치고 나서 사이트에 뜨기까지

1. 런처에서 「세진 과학」 → `켜기.vbs` → `서버.js`(8777) 뜨고 브라우저 열림
2. 화면에서 고친다 (단원 나무·강의 목록은 **화면에서 고치면 서버가 파일에 적는다**)
3. 화면의 **「올리기」** 단추 → `POST /올리기` → 서버가 **판 0.01 올리고** → `git add -A` → commit → push
4. GitHub Pages 가 받아서 **보통 1~3분** 뒤 반영

**반영됐나 확인하는 법** (이게 제일 확실하다):
```
node 검사/자료가따라오나.js
```
또는 직접: `https://sejinedu.github.io/js/판.js` 를 열어 판이 내 것과 같은지 본다.

★ **판을 안 올리면 브라우저가 옛 파일을 계속 쓴다.** `index.html` 이 판으로 캐시를 턴다
(`판.js?t=<시각>` 로 판을 먼저 받고, 나머지는 `?v=<판>`). 그래서 **판 올리기는 선택이 아니다.**
★ `올리기.bat` 은 판을 안 올린다. git add/commit/push 만 한다. **화면 단추 쪽이 정식이다.**

---

## 3. `.gitignore` — 뺀 것과 **절대 못 빼는 것**

**2026-09-11 에 `서버.js`·`켜기.vbs`·`올리기.bat` 을 깃에 넣었다.**
그 전엔 무시돼서 **새 컴퓨터에서 클론해도 켤 수가 없었다.** 넣기 전에 셋 다 쟀다 —
열쇠 0건, 박힌 절대경로·사람 이름 없음(제 자리는 `__dirname`·`GetParentFolderName` 로 스스로 안다).

### ★★★ `자막만들기/` 는 절대 빼지 마라
그 폴더 **안에 `유튜브열쇠.json` 이 있다.** 통째 무시라서 안 올라간다.
**그 안의 파일을 하나씩 추적에 넣지도 마라** — 그 순간 열쇠가 공개 저장소로 샌다.
```
.gitignore:6  자막만들기/     ← 건드리면 열쇠가 샌다
```
검사 `검사/자료가따라오나.js` 가 이걸 **매번 잰다**(막혀 있어야 통과).

---

## 4. 조회수 — 구글 앱스 스크립트

- 코드: `자료/기록서버.gs` (구글에 올려 웹 앱으로 배포한 것)
- 부르는 쪽: `js/기록소.js` — 주소가 **코드에 박혀 있고, 그건 공개 저장소에 이미 있다**
  (`script.google.com/macros/s/AKfycb…`). **열쇠가 아니다** — 구글 웹 앱 공개 주소다.
- **망가지면**: 조회수가 안 모인다. 그것뿐이다. 자막·강의·단원은 멀쩡하다.
  주소가 없거나 서버가 죽으면 **그 브라우저에만 쌓인다**(코드가 그렇게 버틴다).
- 고치려면 구글 앱스 스크립트에서 다시 배포하고 **새 URL 을 `js/기록소.js` 에 넣어야** 한다.

---

## 5. 손으로 고치면 안 되는 파일 — 기계가 적는다

| 파일 | 누가 적나 |
|---|---|
| `js/단원.js` | `서버.js` 의 `/단원나무` (화면에서 나무 고칠 때) |
| `자료/영상목록.js` | `서버.js` 의 `/영상목록` (강의 붙일 때) |
| `자료/길이.json` | `서버.js` 의 `/영상/길이` |
| `js/판.js` | `서버.js` 의 `/올리기` 가 0.01 올린다 |
| `자막/*.js` · `자막/*.srt` | 자막 공장 (아래 6번) |

★★ **`js/단원.js` 머리에 「여기 있는 것이 진짜다. 브라우저 서랍은 거울로 쓴다」 고 적혀 있다.**
이게 이 앱의 뼈대다 — **원본은 파일이고 localStorage 는 캐시다.**
이걸 뒤집으면 「그 컴퓨터에서만 되는 앱」이 된다. 검사가 이 문장이 살아 있는지도 잰다.

---

## 6. 자막 만드는 흐름 (`자막만들기/`, 81MB, 깃에 안 들어감)

지금 자막 **48편 15MB** 는 다 만들어져 있다. 새로 굽는 건 새 강의를 올릴 때뿐이다.

- 받아적기: **Qwen3-ASR-1.7B + Qwen3-ForcedAligner**(로컬 4090). 위스퍼보다 나아서 갈아탔다 —
  같은 강의 같은 자리로 맞대 봤을 때 놓친 말 37초→5초, 도플러 공식이 다 맞았다.
- 사전: `고칠말.txt`(2,400줄) · `공식.txt` · `과학용어.txt` · `말사전.py`
- 다듬기: `고치기.py` → `손보기/말끊기.py`(자막 끊기) → `손보기/시간바로잡기.py`
- 화면에서 「자막 만들기」 를 누르면 `서버.js` 가 `자막만들기/자막.py` 를 python 으로 띄운다.
  **python 이 없으면 그때 죽는다**(보기는 멀쩡하다).

### ★ 자막을 끊는 규칙 — 여기 손대기 전에 꼭 읽어라
사장님이 여러 날 스샷으로 잡아 준 것이다. **글자 수를 세지 않는다.**
- 자막 하나 = **사장님이 3.5초쯤 말한 만큼**. 한 마디가 5.5초 넘을 때만 쪼갠다
- 글씨 크기 = 그 강의에서 **제일 넓은 줄을 실제로 그려 재서** 맞춘다 (`js/자막.js`)
- 마침표는 **화면에서만 뗀다**(속으론 갖고 있다 — 문장 끝을 아는 표다)
- **말 가운데를 자르는 것보다 두 줄이 낫다.** 이건 사장님이 못 박았다

---

## 7. 지뢰 (내가 밟은 것들 — 그대로 밟는다)

1. **★ bash heredoc 이 역슬래시를 먹는다.** `"\\frac{...}"` 를 heredoc 안에 쓰면 깨져서
   화면에 `rac{}` 가 나온다. **정규식·역슬래시가 든 코드는 Edit/Write 도구로 고쳐라.** 여러 번 밟았다.
2. **bash 에서 한글 변수 이름은 안 된다.** `죽은=$(…)` 가 `command not found` 로 튄다. 영문으로 써라.
3. **PowerShell `Stop-Process` 를 한글 `-like` 로 찾으면 안 잡힌다.** 포트를 쥔 PID 를
   `Get-NetTCPConnection -LocalPort 8777` 로 찾아 PID 로 죽여라. 안 그러면 **옛 서버가 계속 답해서
   시험 결과를 거짓으로 읽는다** — 나는 이걸로 두 번 헛다리를 짚었다.
4. **자모를 문자열로 쓰면 절대 안 걸린다.** `"멀어질".endswith("ㄹ")` 은 **False** 다
   (받침 ㄹ 과 낱자 ㄹ 은 다른 글자). `말끊기.py` 의 못자름 목록에 `"ㄹ"` 이 있었는데
   **처음부터 한 번도 작동한 적이 없었다.** 같은 실수를 `"ㅂ니다"` 에서도 밟았다.
5. **사전 규칙이 말 한가운데서 터진다.** 「2법칙 → 제2법칙」을 넣었더니
   「이**제** 2법칙」 이 「이제제2법칙」 이 됐다 — 154줄. **낱말로 덧대지 말고 규칙에서 빼라.**
6. **퀜이 내가 준 용어 목록을 그대로 받아적는 일이 있다**(180자짜리 줄). 뒷단이 잘라낸다.
   다만 사장님이 진짜로 화석 이름을 죽 부르는 자리와 헷갈리면 안 된다 — **내 목록 순서 그대로면** 그게 샌 것이다.
7. `%LOCALAPPDATA%`·`%APPDATA%` 에 클로드가 쓰면 **가상화돼서 진짜 프로세스한테 안 보인다.**
   산출물은 `C:\Users\tpwls\sedobi-local\` 에.
8. `.git` 은 **정션**이다 → 실체 `C:\Users\tpwls\sedobi-local\git\sedobi-homepage\.git`
   (구글 드라이브가 `.git` 을 못 삼켜 오류 13,508개가 났다).

---

## 8. 내가 돌려 본 것 / 안 돌려 본 것

**돌려 보고 눈으로 확인한 것**
- `검사/자료가따라오나.js` 전부 통과 (자료 파일 · 깃 추적 · 공개 서버 200 · 열쇠 막힘)
- **캐시 0개인 새 브라우저**로 공개 사이트를 열어 단원 나무 4뿌리·강의 35개가 뜨는 것 봤다
  → 「드라이브 없이 자료가 따라오나」 는 **이미 통과**다 (Pages 가 곧 서버다)
- 로그인 문지기: 없을 때 보기 200/올리기 401, 있을 때 200, **만료 JWT 는 막힘**
- 빨간 띠가 로컬에선 뜨고 **공개 사이트에선 안 뜨는 것** 눈으로 봤다
- v2.59 · v2.60 실제 push 및 Pages 반영 확인

**안 돌려 본 것 (여기가 남은 위험이다)**
- **런처에서 「세진 과학」 을 실제로 눌러 본 적이 없다.** 나도 런처 방도 안 눌렀다
  (누르면 사장님 화면에 창이 뜬다). **사장님이 로그인한 뒤 한 번 눌러 봐야** 끝이다.
- 새 컴퓨터에서 **진짜로 클론해서 켜 본 적은 없다.** 파일이 따라가는 것까지만 확인했다.
- 만료 검사는 **가짜 JWT** 로 쟀다. 진짜 Supabase 토큰으로는 아직 안 해 봤다.

---

## 9. 남은 것

1. **사장님이 런처에서 로그인** → 그 뒤 올리기가 열린다 (제일 먼저)
2. 런처에서 「세진 과학」 눌러 실제로 켜지는지 확인
3. (안 급함) 런처가 「켜기 직전에 토큰을 새로 받아 넘기기」 — 붙으면 만료가 아예 안 난다
4. 자막 자잘한 오류는 남아 있다. 사장님이 보다가 스샷을 보낸다.
   **규칙으로 고쳐라. 줄 하나씩 손으로 고치면 다음 강의에서 또 난다** — 사장님이 이걸 여러 번 짚었다.

---

## 10. 사장님 대하는 법 (여기가 제일 값지다)

- **코드를 못 읽는다.** 한국어로, **결론부터.** 코드 조각·영어 용어를 들이대지 마라.
- **절차를 시키지 마라.** 「재설치하세요」 「이 명령을 쳐 보세요」 는 답이 아니다.
  내가 재서 판단하고, 사장님은 **「업데이트」 한 번만** 누른다.
- **「고쳤다」 고 말하기 전에 눈으로 봐라.** 숨김 검사 통과는 증거가 아니다.
  안 재고 「됐다」 하면 **스샷이 다시 날아온다.**
- **영상 시험 전엔 반드시 음소거를 먼저 심어라.** 소리가 갑자기 나면 크게 화낸다.
- 끝맺음은 셋으로 — **① 판 번호 ② 뭘 고쳤나 ③ 업데이트 눌러라**(받을 게 있을 때만).
- **내가 틀렸으면 짧게 인정하고 넘어가라.** 변명이나 자책을 길게 하면 더 화낸다.

---
---

# (아래는 그 전 기록 — Codex 세션이 적어 둔 것)

# 2026-09-10 · v2.58 고정 프레임 자막 (공개 배포 완료)

- 사용자 「배포해」 승인 뒤 커밋 `c61fbe8`을 원격 main에 push했다. 공개 https://sejinedu.github.io/ 의 HTML·판·자막·공통 산식·스타일·글꼴/라이선스 8/8이 커밋과 바이트 일치한다. 영상편집 0.53도 R2 게시 및 공개 검증 완료. 사용자 앱·작업은 건드리지 않았다.

- 사용자 실제 YouTube 참조 9AEsTOZ9IIo의 기본 CC는 1920×1080=48px, 960×540=24px이다. 자른 내용 높이 기준을 없애고 영상편집과 같은 1920×1080/48px 공통 프레임으로 표시한다. 창 확대·축소·세로 모양에서도 프레임과 자막을 함께 축척한다.
- Roboto 웹폰트 40,128B 및 OFL 라이선스를 포함하여 영문/공백 폭도 맞췄다. 새 글꼴 로딩 뒤 줄바꿈을 다시 잰다. 기존 크기/높이 선택과 CC 끄기, 자막 문구·시간·영상목록은 보존한다. 기존 게시 YouTube 파일을 수정하지 않는다.
- 영상편집 qa_caption_surfaces-8qx_kaag 8조건, qa_youtube_reference-4tszg2_6 실제 참조 첫 줄 글자 폭 차이 0.032px, 숨김 앱 확대·크롭·DPR 검사 통과. 공개 게시 및 다운로드 검증을 완료했다.

# 2026-09-10 · v2.57 자막 기본 크기

- 영상편집 사용자가 편집기·유튜브 기본 CC·홈페이지 크기를 맞추라고 직접 요청했다. CC 켜기/끄기는 유지하고 기본 100%를 기준으로 통일한다. 기존 웹/폰 배율 및 최장 문장에 따라 크기를 바꾸던 계산을 공통 caption-layout.js로 교체했다. 화면 높이도 다시 재므로 창 크기/전체화면/회전에 대응한다.
- 공통 산식의 개발 원본은 형제 폴더 세도비 영상편집/app/ui/caption-layout.js다. 홈페이지에는 배포 사본을 둔다. 영상편집 qa_caption_surfaces.py가 바이트 일치와 두 실제 브라우저 렌더러의 기본 크기/글꼴/굵기를 대조한다. YouTube 공개 captions.js(2026-09-10) 기본 산식을 참조했고 고정 픽셀 API로 주장하지 않는다.
- 새 홈페이지 연결은 실제 YouTube 업로드 파일의 width/height와 표시 배율을 자막 묶음의 화면에 넣는다. 이미 연결된 편집 영상 9WvMc3S2WNw에도 확인된 1920×710 비율을 추가했다. 자막 줄·문구·시간 SHA256은 그대로다. 화면 정보가 없는 옛 자막은 16:9 기본값이다.
- 새 크기 저장 열쇠는 세진과학.자막크기.v2. 기본 100%, 크기 조절/유지·켜기/끄기·수식 기능 보존. 기본 글꼴/굵기도 YouTube 참고 기준으로 통일했다.
- qa_caption_surfaces-z799tkst: 8가지 화면/자르기/배율, 옛 크기 초기화, 설정 기억, 켜기/끄기 통과 및 스크린샷 시각 확인. 홈페이지 서버·사용자 창·로컬 작업은 조작하지 않았다. 커밋 c12a299를 사용자의 직접 게시 지시 뒤 원격 main에 push했다. 공개 HTML·판·자막 코드·공통 산식·스타일·해당 영상 자막 자료 6/6이 커밋과 바이트 일치한다. 영상편집 0.52도 R2 게시·공개 검증 완료.

